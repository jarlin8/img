"use strict";function gscounteranimate(obj,initVal,lastVal,duration){let startTime=null,currentTime=Date.now();const step=currentTime=>{startTime||(startTime=currentTime);const progress=Math.min((currentTime-startTime)/(1e3*duration),1);obj.innerHTML=Math.floor(progress*(lastVal-initVal)+initVal),progress<1?window.requestAnimationFrame(step):window.cancelAnimationFrame(window.requestAnimationFrame(step))};window.requestAnimationFrame(step)}var gccounter=document.getElementsByClassName("gs-counter");for(let i=0;i<gccounter.length;i++){let counterobj=gccounter[i],start,end,duration;gscounteranimate(counterobj,parseInt(counterobj.dataset.start),parseInt(counterobj.dataset.end),parseFloat(counterobj.dataset.duration))}